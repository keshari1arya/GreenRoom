import { ControlValidationClassDirective } from './control-validation-class.directive';

describe('ControlValidationClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlValidationClassDirective();
    expect(directive).toBeTruthy();
  });
});
