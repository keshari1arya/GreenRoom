version: 0.0
os: linux
files:
  - source: /src/Web/publish/publish.zip
    destination: /var/www/yourapp

hooks:
  ApplicationStart:
    - location: ./.aws/deployment-scripts/app-start.sh
      timeout: 300
      runas: root
  # ValidateService:
  #   - location: ./.aws/scripts/validate-service.sh
  #     timeout: 300
  #     runas: root
